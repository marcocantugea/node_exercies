/**
 * 
 * clase de underscore que tiene funciones muy usadas 
 * 
 */

const underscore= require("underscore");


result = underscore.contains([1,2,3,4,5,6,3],5);

console.log(result);

result_s=underscore.isNumber("e");
console.log(result_s);